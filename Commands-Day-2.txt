for clone code 
git clone https://github.com/Lakshmi-Thirumala/GitCommandPractise.git


even though our repo is public - no one able to push code to our repo -only if we give collabrate access then they will push code


Taking code from github 
git pull or git pull origin master

Branching 

to see all branches
git branch 

for branch creation from existing branch (if we do checkout command on main branch -it creats from main like that)
git checkout -b feature/GitPractise

to switch between branches 
git checkout branchname

to push new branch /branch changes
git push origin -u feature/Git-Practise

to delete branch 
to swithch to another branch and delete branch--local
git branch -d branchname

git branch -D branchname

git push origin --delete  branchName---cloud




git fetch origin (All branch details into local)	


MergeConflicts 

pull code before push or else we landinto merge conflicts on same branch

when we are getting changes from another branch to our branch 

git merge branchname

changes brfore last commit will be gone 

git stash(for uncommitted code)

to restore that again
git stash pop
